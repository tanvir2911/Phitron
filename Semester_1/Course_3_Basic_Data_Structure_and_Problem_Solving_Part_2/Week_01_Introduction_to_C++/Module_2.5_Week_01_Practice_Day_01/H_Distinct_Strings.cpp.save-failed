#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin>>s;

    int n = s.size();

    vector<int>a;
    for(int i=0;i<26;i++)
    {
        a.push_back(i+97);
    }

    for(int i=0;i<a.size();i++)
        cout<<(char)a[i]<<" ";


    if(s[0]==97)
        cout<<"ok";

    return 0;
}
