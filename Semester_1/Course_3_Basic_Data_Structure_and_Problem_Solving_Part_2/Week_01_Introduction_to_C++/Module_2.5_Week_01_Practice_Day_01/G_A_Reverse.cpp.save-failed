#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    long long int a,b;
    string s;
    cin>>a>>b;
    cin>>s;



//    s.reverse(s.begin()+a-1, s.begin()+b);

    cout<<(s.begin()+a-1, s.begin()+b);

    return 0;
}
